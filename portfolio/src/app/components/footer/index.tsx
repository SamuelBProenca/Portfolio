const FooterComponent = () => {
  return (
    <footer className="mt-24 text-center text-neutral-600 text-sm">
      © {new Date().getFullYear()} Samuel Proença. Todos os direitos reservados.
    </footer>
  );
};

export default FooterComponent;
